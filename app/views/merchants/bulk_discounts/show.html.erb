<%= render partial: 'merchants/shared/header' %>

<h2>Bulk Discount <%= @bulk_discount.id %></h2>
<h3><%= @bulk_discount.percentage_discount %>% off when you buy <%= @bulk_discount.quantity_threshold %></h3>
<%= link_to "Edit Discount", edit_merchant_bulk_discount_path(@merchant, @bulk_discount) %>